import { SelfBot } from "../.."
import manager from "../../.."
module.exports = {
    name: "test",
    description: "pipiop",
    execute: async (client: SelfBot, message: any, args: string) => {
    }
}